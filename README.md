# Yakateam Kotatsu Parsers CI
This is an automated ghost repository that periodically polls `Yakateam/kotatsu-parsers` for updates and builds/Dex-packages the `.jar` distribution to the `repo` branch to be served directly to the Kototoro Android Client.
